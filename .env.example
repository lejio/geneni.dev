# Payload
# Generate with: openssl rand -hex 32
PAYLOAD_SECRET=

# Neon Postgres (Vercel Marketplace → Neon → attach to this project)
# Neon / Vercel may inject any of these; Payload accepts all three.
# Prefer sslmode=verify-full (or omit sslmode — payload.config normalizes require → verify-full).
DATABASE_URI=
# DATABASE_URL=
# POSTGRES_URL=

# Optional: public site URL for absolute links later
# NEXT_PUBLIC_SERVER_URL=http://localhost:3000
